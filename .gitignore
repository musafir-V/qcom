# Full-table DynamoDB exports contain production user/OTP data — do not commit.
data/dynamodb-export-*.json
data/migration-manifest.json
data/ssm-export*.json

# AWS credentials — never commit.
.deploy.local.env
.deploy.new.env

# Git worktrees for isolated feature work.
.worktrees/

# Subagent-driven-development scratch: ledgers, task briefs, review packages.
.superpowers/
